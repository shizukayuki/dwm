--- dwm.orig/drw.c
+++ dwm/drw.c
@@ -176,6 +176,8 @@ drw_clr_create(Drw *drw, Clr *dest, cons
 	                       DefaultColormap(drw->dpy, drw->screen),
 	                       clrname, dest))
 		die("error, cannot allocate color '%s'", clrname);
+
+	dest->pixel |= 0xff << 24;
 }
 
 /* Create color schemes. */
